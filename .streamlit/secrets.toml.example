# Copy to `.streamlit/secrets.toml` (never commit the real file)

GEMINI_API_KEY = "your-key-from-aistudio.google.com"

# Local Talis — llama.cpp server (use any free port, e.g. 8086)
# LLAMA_CPP_HOST = "http://127.0.0.1:8086"
